-- DROP TABLE IF EXISTS USERS;
-- create table USERS
-- (
--     ID bigserial primary key,
--     login varchar(30),
--     email varchar(30),
--     password varchar(30),
--     creation_date timestamp,
--     last_change_date timestamp,
--     user_role varchar(30)
-- );
-- create table users (
--     id bigint generated by default as identity,
--     creation_date timestamp,
--     email varchar(255),
--     last_change_date timestamp,
--     login varchar(255),
--     password varchar(255),
--     user_role varchar(255),
--
--     primary key (id)
--
--                    );
-- insert into users(login, email, password, user_role) values('user', '1@gmail', '{noop}password', 'USER');
-- insert into users( login, email, password, user_role) values('admin', '2@gmail', '{noop}password', 'ADMIN');
Select * from users;