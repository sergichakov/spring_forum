server:
  application:
    name: topic-server
  port: 8083
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/topicsdb # ##jdbc:postgresql://${DB_SERVER}/${POSTGRES_DB}
    username: jira # ##${POSTGRES_USER}
    password: JiraRush # ##${POSTGRES_PASSWORD}
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  kafka:
    bootstrap-servers: localhost:9092
    consumer: 
      auto-offset-reset: earliest
      group-id: topic-server
  liquibase:
    #change-log: classpath:/db/changelog/db.changelog-master.xml
    changeLog: "classpath:db/changelog.sql"
  jpa:
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true
        hbm2ddl:
          auto: update # #create-drop

    show-sql: true

kafka:
  topic:
    product:
      request: topic-req-topic
      reply: topic-req-reply-topic
  request-reply:
    timeout-ms: 10000
product:
  topic:
    request:
      numPartitions: 1 # ##### numPartitions: 5
logging:
  level:
    root: info # info

